import { InputDebounceDirective } from './input-debounce.directive';

describe('InputDebounceDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDebounceDirective();
    expect(directive).toBeTruthy();
  });
});
