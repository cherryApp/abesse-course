import { ServerValidatorDirective } from './server-validator.directive';

describe('ServerValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ServerValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
